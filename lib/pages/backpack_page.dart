import 'package:flutter/material.dart';

class BackpackPage extends StatelessWidget {
  const BackpackPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('backpack'),),
    );
  }
}