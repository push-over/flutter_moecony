import 'package:flutter/material.dart';

class ActivityTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('ActivityTab'),
      ),
    );
  }
}
